<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="mx-3"><h3 class="mb-5 mini-title">プロフィール編集</h3></div>
    <div class="col-10 col-md-10 col-sm-10 mx-2 mx-sm-auto px-0 my-5">
      <%= form_with model: @user, local: true do |f| %>

        <div class="form-group">
          <%= f.label :登録中の画像 %>
          <%= image_tag @user.get_profile_image, class: "my_page_img rouded-circle" %>
        </div>

        <div class="form-group">
          <%= f.label :変更する画像 %>
          <%= f.file_field :profile_image, class: "form", accept: "image/*" %>
        </div>

        <div class="form-group">
          <%= f.label :screen_name, "ニックネーム" %>
          <%= f.text_field :screen_name, class: "form" %>
        </div>

        <div class="form-group">
          <%= f.label :introduction, "自己紹介" %>
          <%= f.text_area :introduction, class: "form" %>
        </div>

        <div class="form-group">
          <%= link_to "キャンセル", user_path(current_user), class: "back" %>
          <%= f.submit "変更する", class: "form" %>
        </div>
      <% end %>

      <div class="withdraw">
        <%= link_to "退会する", unsubscribe_path %>
      </div>
    </div>
  </div>
</div>